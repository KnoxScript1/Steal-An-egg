-- ============================================================
-- LEAKED BY BJ AND RICK HUB
-- ============================================================
local UIS = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local Lighting = game:GetService("Lighting")
local ProximityPromptService = game:GetService("ProximityPromptService")
local Player = Players.LocalPlayer
local PlayerGui = Player:WaitForChild("PlayerGui")
local Backpack = Player:WaitForChild("Backpack")
if PlayerGui:FindFirstChild("ElevenHub") then PlayerGui.ElevenHub:Destroy() end
local ElevenHub = Instance.new("ScreenGui")
ElevenHub.Name = "Knox Hub"
ElevenHub.Parent = PlayerGui
ElevenHub.ResetOnSpawn = false
local targetSpeed = 28.5
local speedEnabled = false
local jumpEnabled = false
local JUMP_VELOCITY = 65
local megaphoneEnabled = false
local antiRagdollEnabled = false
local spinning = false 
local spinSpeed = 50 
local shiftLockEnabled = false
local function runDevourLag()
    task.wait(1)
    Lighting.GlobalShadows = false
    Lighting.EnvironmentDiffuseScale = 0
    Lighting.EnvironmentSpecularScale = 0
    for _, v in ipairs(Lighting:GetChildren()) do if v:IsA("PostEffect") then v.Enabled = false end end
    for _, v in ipairs(workspace:GetDescendants()) do if v:IsA("ParticleEmitter") then v.Enabled = false end end
    local function cleanCharacter(char)
        if char == Player.Character then return end
        for _, a in ipairs(char:GetChildren()) do if a:IsA("Accessory") then a:Destroy() end end
        char.ChildAdded:Connect(function(c) if c:IsA("Accessory") then c:Destroy() end end)
    end
    for _, h in ipairs(workspace:GetDescendants()) do if h:IsA("Humanoid") then cleanCharacter(h.Parent) end end
    -- Ladder Logic
    local LADDER_NAME = "Ladder"
    local savedLadder
    local function findLadder()
        for _, v in ipairs(workspace:GetDescendants()) do if v.Name == LADDER_NAME and v:IsA("Model") then return v end end
    end
    local function saveLadder()
        if savedLadder then return end
        local ladder = findLadder()
        if ladder then savedLadder = ladder:Clone(); savedLadder.Parent = nil end
    end
    local function spawnLadder()
        if not savedLadder then return end
        local char = Player.Character
        if not char then return end
        local hrp = char:WaitForChild("HumanoidRootPart", 5)
        if not hrp then return end
        local ladder = savedLadder:Clone()
        ladder.Parent = workspace
        if ladder.PrimaryPart then ladder:SetPrimaryPartCFrame(hrp.CFrame * CFrame.new(0, 0, -7)) end
    end
    saveLadder()
    spawnLadder()
    -- Auto Devour
    local char = Player.Character
    if char then
        local humanoid = char:FindFirstChildOfClass("Humanoid")
        local tool = Backpack:FindFirstChild("Quantum Cloner")
        if humanoid and tool then
            humanoid:EquipTool(tool)
            task.wait(0.1)
            for _, t in ipairs(Backpack:GetChildren()) do if t:IsA("Tool") then t.Parent = char end end
            task.wait(0.1)
            tool:Activate()
        end
    end
end
local function setupSpin(char)
    local hrp = char:WaitForChild("HumanoidRootPart")
    local existing = hrp:FindFirstChild("ElevenSpin")
    if existing then existing:Destroy() end
    local bav = Instance.new("BodyAngularVelocity")
    bav.Name = "ElevenSpin"
    bav.MaxTorque = Vector3.new(0, math.huge, 0)
    bav.P = 100000
    bav.AngularVelocity = Vector3.new(0, 0, 0)
    bav.Parent = hrp
end
Player.CharacterAdded:Connect(setupSpin)
if Player.Character then setupSpin(Player.Character) end
UIS.JumpRequest:Connect(function()
    if not jumpEnabled then return end
    local char = Player.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if hrp then
        local v = hrp.AssemblyLinearVelocity
        hrp.AssemblyLinearVelocity = Vector3.new(v.X, JUMP_VELOCITY, v.Z)
    end
end)
local MainFrame = Instance.new("Frame", ElevenHub)
MainFrame.Size = UDim2.new(0, 320, 0, 250)
MainFrame.Position = UDim2.new(0.5, -160, 0.5, -125)
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
MainFrame.Visible = false
Instance.new("UICorner", MainFrame)
local Sidebar = Instance.new("Frame", MainFrame)
Sidebar.Size = UDim2.new(0, 85, 1, 0)
Sidebar.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
Instance.new("UICorner", Sidebar)
local TabContainer = Instance.new("Frame", Sidebar)
TabContainer.Position = UDim2.new(0, 0, 0, 50)
TabContainer.Size = UDim2.new(1, 0, 1, -50)
TabContainer.BackgroundTransparency = 1
Instance.new("UIListLayout", TabContainer).HorizontalAlignment = Enum.HorizontalAlignment.Center
local Pages = Instance.new("Frame", MainFrame)
Pages.Position = UDim2.new(0, 95, 0, 10)
Pages.Size = UDim2.new(1, -105, 1, -20)
Pages.BackgroundTransparency = 1
local function CreateTabPage(name)
    local PageFrame = Instance.new("Frame", Pages)
    PageFrame.Name = name .. "Page"
    PageFrame.Size = UDim2.new(1, 0, 1, 0)
    PageFrame.BackgroundTransparency = 1
    PageFrame.Visible = false
    local Nav = Instance.new("Frame", PageFrame)
    Nav.Size = UDim2.new(1, 0, 0, 32)
    Nav.BackgroundTransparency = 1
    local LBtn = Instance.new("TextButton", Nav)
    LBtn.Size = UDim2.new(0.48, 0, 1, 0)
    LBtn.Text = "Left"
    LBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
    LBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    Instance.new("UICorner", LBtn)
    local RBtn = Instance.new("TextButton", Nav)
    RBtn.Position = UDim2.new(0.52, 0, 0, 0)
    RBtn.Size = UDim2.new(0.48, 0, 1, 0)
    RBtn.Text = "Right"
    RBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
    RBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    Instance.new("UICorner", RBtn)
    local LeftCat = Instance.new("Frame", PageFrame)
    LeftCat.Position = UDim2.new(0, 0, 0, 40)
    LeftCat.Size = UDim2.new(1, 0, 1, -40)
    LeftCat.BackgroundTransparency = 1
    Instance.new("UIListLayout", LeftCat).Padding = UDim.new(0, 5)
    local RightCat = Instance.new("Frame", PageFrame)
    RightCat.Position = UDim2.new(0, 0, 0, 40)
    RightCat.Size = UDim2.new(1, 0, 1, -40)
    RightCat.BackgroundTransparency = 1
    RightCat.Visible = false
    Instance.new("UIListLayout", RightCat).Padding = UDim.new(0, 5)
    LBtn.MouseButton1Click:Connect(function() LeftCat.Visible = true; RightCat.Visible = false end)
    RBtn.MouseButton1Click:Connect(function() LeftCat.Visible = false; RightCat.Visible = true end)
    return PageFrame, LeftCat, RightCat
end
local MainPage, MainLeft, MainRight = CreateTabPage("Main")
local DuelsPage, DuelsLeft, DuelsRight = CreateTabPage("Duels")
local SettingsPage, SettingsLeft, SettingsRight = CreateTabPage("Settings")
MainPage.Visible = true
local function MakeTabButton(name, targetPage)
    local b = Instance.new("TextButton", TabContainer)
    b.Size = UDim2.new(0.85, 0, 0, 30)
    b.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
    b.Text = name
    b.TextColor3 = Color3.fromRGB(255, 255, 255)
    Instance.new("UICorner", b)
    b.MouseButton1Click:Connect(function()
        for _, p in pairs(Pages:GetChildren()) do if p:IsA("Frame") then p.Visible = false end end
        targetPage.Visible = true
    end)
end
MakeTabButton("Main", MainPage)
MakeTabButton("Duels", DuelsPage)
MakeTabButton("Settings", SettingsPage)
local SpeedBtn = Instance.new("TextButton", MainLeft)
SpeedBtn.Size = UDim2.new(1, 0, 0, 32)
SpeedBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
SpeedBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
Instance.new("UICorner", SpeedBtn)
SpeedBtn.MouseButton1Click:Connect(function() speedEnabled = not speedEnabled end)
local JumpBtn = Instance.new("TextButton", MainLeft)
JumpBtn.Size = UDim2.new(1, 0, 0, 32)
JumpBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
JumpBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
Instance.new("UICorner", JumpBtn)
JumpBtn.MouseButton1Click:Connect(function() jumpEnabled = not jumpEnabled end)
local MegaphoneBtn = Instance.new("TextButton", MainLeft)
MegaphoneBtn.Size = UDim2.new(1, 0, 0, 32)
MegaphoneBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
MegaphoneBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
Instance.new("UICorner", MegaphoneBtn)
MegaphoneBtn.MouseButton1Click:Connect(function() megaphoneEnabled = not megaphoneEnabled end)
local AntiRagBtn = Instance.new("TextButton", MainLeft)
AntiRagBtn.Size = UDim2.new(1, 0, 0, 32)
AntiRagBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
AntiRagBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
Instance.new("UICorner", AntiRagBtn)
AntiRagBtn.MouseButton1Click:Connect(function() antiRagdollEnabled = not antiRagdollEnabled end)
local DevourBtn = Instance.new("TextButton", MainRight)
DevourBtn.Size = UDim2.new(1, 0, 0, 32)
DevourBtn.Text = "Devour (Lag)"
DevourBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
DevourBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
Instance.new("UICorner", DevourBtn)
DevourBtn.MouseButton1Click:Connect(function() runDevourLag() end)
local SpinBtn = Instance.new("TextButton", DuelsLeft)
SpinBtn.Size = UDim2.new(1, 0, 0, 32)
SpinBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
SpinBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
Instance.new("UICorner", SpinBtn)
SpinBtn.MouseButton1Click:Connect(function() spinning = not spinning end)
local SLockBtn = Instance.new("TextButton", SettingsLeft)
SLockBtn.Size = UDim2.new(1, 0, 0, 32)
SLockBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
SLockBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
Instance.new("UICorner", SLockBtn)
SLockBtn.MouseButton1Click:Connect(function() shiftLockEnabled = not shiftLockEnabled end)
RunService.RenderStepped:Connect(function()
    local char = Player.Character
    if not char then return end
    local hum = char:FindFirstChildOfClass("Humanoid")
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local bav = hrp and hrp:FindFirstChild("ElevenSpin")
    if spinning and bav then
        bav.AngularVelocity = Vector3.new(0, spinSpeed, 0)
        hum.AutoRotate = false
    elseif bav then
        bav.AngularVelocity = Vector3.new(0, 0, 0)
        hum.AutoRotate = true
    end
    SpinBtn.Text = "Spinbot: " .. (spinning and "ON" or "OFF")
    SpeedBtn.Text = "Speed: " .. (speedEnabled and "ON" or "OFF")
    JumpBtn.Text = "Jump: " .. (jumpEnabled and "ON" or "OFF")
    MegaphoneBtn.Text = "Megaphone: " .. (megaphoneEnabled and "ON" or "OFF")
    SLockBtn.Text = "Shift Lock: " .. (shiftLockEnabled and "ON" or "OFF")
    AntiRagBtn.Text = "Anti-Ragdoll: " .. (antiRagdollEnabled and "ON" or "OFF")
    Player.DevEnableMouseLock = shiftLockEnabled
    if antiRagdollEnabled and hum then
        hum.PlatformStand = false
        hum:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, false)
        hum:SetStateEnabled(Enum.HumanoidStateType.FallingDown, false)
    end
end)
RunService.Heartbeat:Connect(function()
    local char = Player.Character
    if not char or not speedEnabled then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChildOfClass("Humanoid")
    if hrp and hum and hum.MoveDirection.Magnitude > 0 then
        hrp.Velocity = Vector3.new(hum.MoveDirection.X * targetSpeed, hrp.Velocity.Y, hum.MoveDirection.Z * targetSpeed)
    end
end)
local OpenBtn = Instance.new("TextButton", ElevenHub)
OpenBtn.Size = UDim2.new(0, 120, 0, 35)
OpenBtn.Position = UDim2.new(0, 20, 0.5, -17)
OpenBtn.BackgroundColor3 = Color3.fromRGB(80, 150, 255)
OpenBtn.Text = "🍀 KNOX HUB"
OpenBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
Instance.new("UICorner", OpenBtn)
OpenBtn.MouseButton1Click:Connect(function() MainFrame.Visible = not MainFrame.Visible end)
local function MakeDrag(gui)
    local dragging, dragStart, startPos
    gui.InputBegan:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = true; dragStart = input.Position; startPos = gui.Position end end)
    UIS.InputChanged:Connect(function(input) if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then local delta = input.Position - dragStart; gui.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y) end end)
    UIS.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end end)
end
MakeDrag(MainFrame)
MakeDrag(OpenBtn)
